spring:
  boot:
    admin:
      client:
        url: http://127.0.0.1:8888
  cloud:
    config:
      enabled: false
      server:
        encrypt:
          enabled: false

encrypt:
  keyStore:
    location: classpath:keystore.jks
    password: letmein
    alias: mytestkey
    secret: changeme